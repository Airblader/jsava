qx.Interface.define( 'jsava.util.Iterator', {
    members: {
        /**
         * @returns {Boolean}
         */
        hasNext: function () {
        },

        /**
         * @returns {*}
         */
        next: function () {
        },

        remove: function () {
        }
    }
} );