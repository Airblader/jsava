qx.Interface.define( 'jsava.lang.Comparable', {
    members: {
        /**
         * @public
         * @param other
         * @returns {Number}
         */
        compareTo: function (other) {
        }
    }
} );